# Plantilla de configuración. Crea tu .env con:  cp .env.example .env
# y pon una contraseña en POSTGRES_PASSWORD. El .env NUNCA se commitea (.gitignore).
#
# Son credenciales de DESARROLLO para un Postgres local, no un secreto de producción.
POSTGRES_DB=libro
POSTGRES_USER=app
POSTGRES_PASSWORD=
